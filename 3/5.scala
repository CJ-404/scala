object Main extends App{
    def program(a:Int,b:Int)={
        if(a==1)
        if(b==1)
        println("***");
        else
        println("###");
        println("===");
    }

    program(1,2);
    program(1,3);
    program(2,3);
    program(2,2);
}